This directory contains configuration required to run the complete
Authen::Kerberos test suite.  If there is no configuration in this
directory, some of the tests will be skipped.  To enable the full test
suite, create the following files:

keytab

    A valid Kerberos keytab for a principal, preferrably in your local
    realm.  This will be used to test APIs that require a full
    authentication against a KDC or require current tickets.
